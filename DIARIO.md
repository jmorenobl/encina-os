# Diario de Encina OS

- **2026-08-07** — A1 terminada. encina-branding 0.1.6 verificado en VM: 10/10 de la definicion de terminado, cuatro de ellas mirando la pantalla (splash, GDM y fondo). Seis versiones de correccion (0.1.1 a 0.1.6), cuatro de ellas fallos silenciosos: perfil de dconf de GDM, secciones :ubuntu del gschema.override, logo.png fuera del directorio del tema de Plymouth y latido imperceptible del logotipo. Repositorio privado jmorenobl/encina-os creado con CI. Siguiente: comprobar que la CI sale verde y empezar A2 (encina-firefox-native).
- **2026-08-07 (correccion)** — La entrada anterior decia «12/12» y «seis fallos silenciosos»; son diez comprobaciones y cuatro fallos silenciosos. Corregida en el sitio para que no se propague: el dato malo ya habia llegado al README.
- **2026-08-07** — CI verde por push, confirmado: run 31130097305 sobre 9a673b8. Queda cerrada la duda que arrastraba el flujo desde la incidencia de Actions del 06-08. El disparo por push SI funciona; lo que fallaba era la entrega: doce minutos entre el push y la creacion de la ejecucion, y el push de ffbb7fd nunca genero ninguna. Moraleja para la proxima vez: si tras un push no aparece ejecucion, no toques build.yml, espera y tira de workflow_dispatch. Escrito el README raiz y puestos al dia ENCINA-OS.md, DIARIO.md y el README del paquete. Siguiente: A2 (encina-firefox-native), especificado en AGENTS.md §5.
- **2026-08-07** — A2 terminada. encina-firefox-native 0.2.0, 7/7 de AGENTS.md 5.5 verificado en VM. Configura el repositorio de Mozilla con la clave verificada por huella y el anclaje, y hace que el icono abra Firefox nativo. Esto ultimo no estaba en la especificacion: con las siete comprobaciones automaticas en verde, el icono seguia abriendo el Snap, y estaba en espanol, asi que parecia correcto. Se vio en about:support. AGENTS.md 5.1 enmendado con el motivo (D3 manda sobre la delegacion de R4 a la receta de imagen) y anadida D11: el duplicado de lanzadores se resuelve quitando el Snap en la ISO propia, no ocultando entradas, porque NoDisplay borro el icono del dock de una sesion en marcha. Cuatro fallos propios de los scripts, todos falsos negativos, documentados en SCRIPTS.md. Siguiente: decidir si se abre A3 (encina-locale-es), que sigue listado como fuera de alcance.
- **2026-08-07** — A3 suprimida sin escribir una linea de codigo. Se midio antes de abrirla y no habia nada que hacer: check-language-support -l es devuelve vacio, y --show-installed confirma que los siete paquetes de soporte de espanol ya estan. El motivo es que el instalador de Ubuntu ejecuta ese mismo comando al final de la instalacion y actua sobre su salida: esta en subiquity-server-debug.log, instalando hunspell-es y wspanish el solo. En pkg_depends la unica regla especifica de espanol en 184 lineas es wa:es::wspanish. Ademas chocaba con R5: /etc/default/keyboard, /etc/locale.gen y /etc/default/locale no son conffiles de nadie, los genera debconf. El unico hueco real es que las aplicaciones instaladas despues del sistema no reciben su l10n (apt-get -s install libreoffice-writer no arrastra libreoffice-l10n-es), y eso son tres Depends de encina-meta. Anadida D12 y la seccion 6.1 con las salidas literales; puestos al dia AGENTS.md 7, README y la hoja de ruta. La leccion no es sobre locales: A3 llevaba meses en la hoja de ruta y sobrevivio porque nadie habia ejecutado el comando que la propia casilla proponia como criterio. Antes de abrir cualquier fase, preguntar que comando demuestra que el problema existe. Hecho tambien un clon UTM encina-A2-verificada del estado bueno de A2, que no existia. Cabo suelto comprobado y descartado, el mismo dia: debconf-show keyboard-configuration decia layoutcode us mientras /etc/default/keyboard decia es, porque curtin escribio el fichero sin pasar por debconf. Parecia una trampa —que un dpkg-reconfigure devolviera el teclado a ingles— y NO lo es. Ejecutado dpkg-reconfigure -f noninteractive keyboard-configuration en la VM: el fichero no se movio (diff vacio, XKBLAYOUT sigue es) y lo que se corrigio fue debconf, que paso a Spanish/es. El motivo esta en keyboard-configuration.config linea 67: CONFIGFILE=/etc/default/keyboard, el script siembra debconf desde el fichero, asi que manda el fichero y la discrepancia se cura sola. No se escribe nada en la seccion 9: no hay trampa. Importaba porque esa VM se instalo por autoinstall, que es lo que A5 va a distribuir; queda descartado tambien para A5. De regalo, la reconfiguracion regenero el initramfs y sirvio de reverificacion de A1: los seis ficheros del tema encina siguen dentro, logo.png incluido, y la alternativa default.plymouth sigue apuntando a encina. Siguiente: decidir entre encina-meta (A4 reducida, sin el repo firmado) o comprobar ya la hipotesis de la Etapa B instalando el deb oficial de AutoFirma en la VM, que es lo que puede cambiar todo lo demas.
- **2026-08-07** — Comprobada la hipotesis central de la Etapa B en maquina propia. Sale que SI: la Etapa B esta justificada y encina doctor tiene hueco real. Instalado el deb oficial de AutoFirma 1.9 de firmaelectronica.gob.es en la VM de A2 (Firefox 153 nativo, el caso favorable por construccion) y probada una firma real en la sede de la DGSFP. Autenticidad verificada antes de instalar y mejor de lo esperado: el deb va firmado con dpkg-sig por la Secretaria General de Administracion Digital, la firma verifica, y postinst y preinst son byte a byte identicos a la fuente publica de ctt-gob-es/clienteafirma. No hay nada turbio; lo que hay es ingenieria descuidada. La pagina oficial no publica ni hash ni firma ni clave: la clave la encontre en keyserver.ubuntu.com. Cinco fallos encadenados, cada uno capaz de esconder al siguiente. Uno: el JRE no se declara, y no es un olvido sino una errata, Recoments en vez de Recommends, asi que dpkg lo arrastra como campo de usuario y ni --install-recommends lo salva. Dos: sin Java el postinst encadena ocho comandos fallidos, imprime igualmente que ha instalado el certificado, y termina en install ok installed con codigo 0, porque es sh sin set -e y con exit 0 incondicional. Tres: instalar Java despues no repara nada, el postinst ya corrio y nada lo vuelve a lanzar. Cuatro: con Java presente, la CA del socket va al perfil del Snap y no al del Firefox nativo; lo confirma el propio desinstalador que AutoFirma se genera, que apunta solo al Snap. Cinco: Firefox de Mozilla no lee /etc/firefox/pref, asi que las tres preferencias network.protocol-handler.*.afirma no existen, medido en ejecucion sobre un Firefox vivo. Y el almacen del sistema tampoco vale: enterprise_roots esta en true, la CA esta en /etc/ssl/certs, y Firefox ve 167 certificados y ninguno de AutoFirma. En la prueba real sale el dialogo de la sede diciendo que no puede conectar, y AutoFirma NUNCA llega a arrancar: ni proceso java, ni nada escuchando en el socket, ni su propio log tocado. El remedio que propone ese mismo dialogo, Herramientas -> Restaurar instalacion, responde que ya esta todo bien y sale con codigo 0 dejando el perfil nativo intacto, porque comprueba que exista un fichero en /usr/lib/Autofirma y no que el navegador tenga la CA. Eso es el argumento de existencia de encina doctor en una sola pantalla. Lo importante para el proyecto es la correccion de la seccion 3: A2 NO elimino el obstaculo principal de la Etapa B, lo desplazo. Firefox nativo sigue siendo correcto, pero AutoFirma falla ahi MAS que en una Ubuntu de fabrica, donde al menos acierta con el perfil del Snap, y al quitar el Snap en la ISO propia (D11, R4) se queda sin ningun perfil. La Etapa B deja de ser deseable para ser consecuencia necesaria de A2. Dos lecciones de metodo. Una: una deduccion bien fundada puede acertar el fallo y errar la fase. Yo habia deducido que fallaria el TLS del socket, y la CA si esta medidamente ausente, pero el fallo ocurre antes, en el handler de protocolo. Las dos cosas son ciertas y solo una es la causa: lo medido y lo deducido no se mezclan en el mismo parrafo. Dos: que un error sea visible no lo hace diagnosticable. El usuario ve un dialogo, hace exactamente lo que le dice, y el sistema le contesta con codigo 0 que todo esta bien. Mirado en pantalla: NO aparecio ningun dialogo de abrir con antes del error, asi que al usuario no se le llego a ofrecer nada. Cerrado el mismo dia con la sesion grafica abierta: el handler del SISTEMA si funciona, xdg-open afirma:// arranca AutoFirma, abre ventana, interpreta el URI y se pone a escuchar en el 63117, y con una version de protocolo invalida da un dialogo de error claro (SAF_21). El eslabon roto es solo Firefox, que no entrega el URI. Y de ahi salio lo mas util del dia para B1: las barreras son DOS e independientes. El socket es TLS, CN=127.0.0.1 emitido por CN=Autofirma ROOT, comprobado con openssl s_client, y un handshake en claro recibe alerta fatal; asi que el navegador tambien tiene que confiar en esa CA, que el Firefox nativo no tiene. Arreglar solo el esquema afirma produciria un sistema que sigue sin firmar y ya sin sintoma nuevo que seguir. Esto matiza la leccion de metodo: mi deduccion inicial (fallaria el TLS del socket) no era falsa, era la SEGUNDA de la cola. Dos falsos negativos propios ese rato, los dos por el metodo y no por lo comprobado: un Cant connect to X11 window server que era mi sesion ssh sin XAUTHORITY, y un pkill -f que mato la propia sesion que lo ejecutaba porque el patron casaba con su linea de ordenes, que es la trampa 3 de SCRIPTS.md otra vez. Y un dialogo de error de AutoFirma que parecia hallazgo y lo habia provocado yo inventandome v=1 en el URI. NO medido y no dado por bueno: el SEC_ERROR_ADDING_CERT del issue 459, que no se reprodujo en cinco instalaciones ni forzandolo a mano, asi que la seccion 4 ya no lo afirma; y Chrome, que no esta instalado. El issue 302 en cambio queda confirmado y ampliado. Puestos al dia ENCINA-OS.md (seccion 3 corregida, 4 reescrita con 4.1 nueva, 7, 8, 9 con cinco trampas nuevas y 10) y el README, que repetia la afirmacion desmentida en dos sitios. Clonada la VM como encina-autofirma-rota: no es basura, es el caso de prueba numero uno de B1, con la cadena causal medida de punta a punta, y reproducirla cuesta una tarde. Anadida D13: ninguna de las dos barreras se arregla desde encina-firefox-native. No es disciplina de alcance, es una asimetria medida. La barrera 1 (el esquema afirma) SI cabria en ese paquete, es declarativa, va en /usr/lib/firefox/distribution y pasaria lintian; la 2 no cabe de ninguna manera porque la CA la genera el postinst de AutoFirma, es distinta en cada maquina y vive en el perfil del usuario, que es lo que R1 prohibe. Y cerrar solo la 1 es PEOR que no cerrar ninguna: hoy el usuario ve un dialogo, desorientador pero sintoma; si se cierra la 1, AutoFirma arrancara, el navegador rechazara el certificado y se quedara sin firmar y sin aviso. Cambiar un fallo con sintoma por uno sin el es un retroceso en un proyecto cuyo producto es el diagnostico. Puesta ademas donde estara la mano y no solo en el indice: AGENTS.md 5.1, que es lo que se lee al ir a tocar el paquete, porque la tentacion no llegara como "voy a implementar AutoFirma" sino como "anado una preferencia a encina-firefox-native", que suena a A2 y no dispara ninguna alarma. Siguiente: decidir que fase se abre. encina-meta (A4 reducida) ya no es la pregunta interesante; lo que la medicion ha desbloqueado es B1.
