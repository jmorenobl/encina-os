# 4.80 LA FASE 2, LA REFACTORIZACIÓN ENTERA: las trece tareas que quedaban, cada una con su definición de terminado ejecutada (2026-08-28)

**Qué es esto.** El registro de la fase 2 de `TAREAS.md` —el bloque
[refactorizacion.md](../tareas/cerradas/refactorizacion.md) entero, tareas 2 a 11 y 14
a 16; la 1, la 12 y la 13 ya estaban hechas—, ejecutado de seguido el
2026-08-28 sobre `main` en `794f57d`, con el árbol limpio y la fase 1
completa (§4.79). **Ni un byte del producto**: no se ha arrancado ninguna VM
—`encina-dev` estuvo apagada todo el día y no se encendió—, no se ha tocado
ningún medio de `medios/`, y las dos huellas vigentes (`3d5d12a9…` `amd64`,
`63f360dd…` `arm64`) siguen siendo las de §4.77 y §4.79: la tarea 11 lo
demuestra con su control, (l).

Un apartado por tarea, en el orden en que se ejecutaron —que es el que pide el
propio bloque: la 11 la última, con el producto congelado—. Lo que aquí no
tiene apartado no está hecho. **Los bancos son la red y se pasaron antes y
después de cada movimiento**; sus líneas base están en (a).

#### (a) Las líneas base, tomadas ANTES de tocar nada

Sobre `794f57d`, árbol limpio:

```
bancos/enlaces.sh          correctas: 9   fallos: 0   avisos: 5   omitidas: 2   (2414 referencias §, 447 cualificadas)
imagen/banco-cadena.sh     correctas: 8   fallos: 0
scripts/banco-veredicto.sh correctas: 9   fallos: 0
imagen/banco-mecanismos.sh correctas: 1   fallos: 0   omitidas: 5   [AVISO] 5 medios no estaban
veredicto-conteo.py --banco  correctas: 8   fallos: 0
fabricar-iso.sh --leer-mecanismos medios/encina-os-arm64.iso   -> 1 1 1 1
comprobar-propios.sh encina-branding   -> [OK] 6929df0e69ac…  6952548 bytes
```

`banco-autosuficiencia.sh` queda `[OMIT]` todo el día: exige la VM constructora.
Al final de la fase, con el árbol entero movido, los mismos bancos dan los
mismos recuentos —y tres más que no existían—: (m).

#### (b) Tarea 2, la mitad de `imagen/` que §4.67 dejó `[OMIT]`

Los seis guiones de `imagen/` que abortaban —`fabricar-iso.sh`,
`construir-todo.sh`, `traer-iso-oficial.sh`, `cosechar-repo.sh`,
`fabricar-seed.sh`, `comprobar-propios.sh`— renombran su `fallo()` a `morir()`,
definición y llamadas, y `morir()` escribe por `stderr` como en `lib.sh` y en
`capa-marca.sh`. Doce guiones de `imagen/` llevan la línea `MODELO DE SALIDA`
(seis ABORTAR, seis CONTAR Y SEGUIR). **`imagen/encina-seed.sh` no se toca**:
viaja en la ISO en base64 y un comentario cambia los bytes del medio (§4.67e).

Dos cosas que salieron al hacerlo:

- **El `sed` de macOS (BSD) no entiende `\b`.** La primera pasada,
  `s/\bfallo "/morir "/`, no cambió **nada** en los seis —0 de 137 en
  `fabricar-iso.sh`— y sólo se vio porque se contó (`grep -c 'morir "'` dio 0).
  Con el literal `fallo "` sí.
- **El hook de `rtk` filtra `diff` también en una tubería**: `diff a b | head`
  imprimió «`[ok] Files are identical`» sobre dos ficheros que `cmp -s` decía
  distintos. Con `/usr/bin/diff` se vio la diferencia real: una línea, el
  recuento de referencias cualificadas de `enlaces.sh`, 447 → 459, **exactamente
  las doce cabeceras nuevas** citando §4.67.

Control negativo de `morir()`: `eval` de la definición y `morir "probando"` →
`rc=1`, `stderr=[[FALLO] probando]`, y el `echo` de detrás **no se ejecuta**.
Después: los cuatro bancos y `--leer-mecanismos` idénticos byte a byte a (a);
`grep -rn '^\s*fallo()'` enseña seis definiciones y **las seis cuentan y
siguen**. `[OMIT]` que siguen: las 20 guardas mudas de §4.67d (guiones de VM) y
ejecutar los guiones de VM.

#### (c) Tarea 3, `lib/salida.sh` y `lib/vm.sh`

`lib/salida.sh` (143 líneas): el vocabulario, los contadores, `fallo()`/`morir()`,
`comprobar*()`, `dato()`, `ojos()` y `resumen()`, **sin `set`** —los guiones de
`imagen/` corren sin `-e` y un `set` en la biblioteca les cambiaría el flujo—.
`lib/vm.sh` (216): `ENCINA_LIB_DIR`, la carga de `salida.sh`, y desde
«auxiliares» el texto de `lib.sh` **verbatim** (`raiz_repo`, `requiere_*`,
`xdg_data_dirs_sesion`, `resolver_desktop`, `PKG_DIR`, el vigilante).
`scripts/lib.sh` queda de **puente** —`set -euo pipefail` y `. lib/vm.sh`— para
los 17 que lo cargan, que no se tocan.

El control, como en §4.67a: `declare -F` + `set +o` + `raiz_repo` con el
`lib.sh` viejo (copiado a un árbol de mentira con su `AGENTS.md`) contra el
puente nuevo → las 27 opciones idénticas, las mismas funciones salvo las dos
**nuevas** (`dato`, `ojos`), y `raiz_repo` da la raíz de cada árbol. Negativo:
`unset -f morir` en el nuevo y el `diff` lo ve.

Parcheados con un `python` que exige que cada trozo sustituido sea **único**:
los seis que abortan, los cuatro que cuentan, los tres bancos —`banco-cadena` y
`banco-veredicto` con su `comp()` sobre `ok`/`fallo`; `banco-mecanismos` con el
`printf` dentro de `ok`/`fallo`/`omitido`— y `bancos/enlaces.sh`.
**`grep -rn '^\s*(ok|fallo|aviso|omitido)()'` fuera de `lib/`: nada.** Los
recuentos: `banco-cadena` 8/0, `banco-veredicto` 9/0, `banco-mecanismos`
1/0/5, `enlaces` 9/0, `comprobar-propios` con la única diferencia de la sangría
del `[OK]`. `enlaces.sh` avisó solo de que `lib/salida.sh` y `lib/vm.sh`
sobraban de su lista de exclusión: es para lo que existe el aviso.

**`verificar-instalacion.sh` tiene un problema que los demás no tienen: corre
dentro de la máquina instalada, sin el clon** (llega por `wget` desde el
buzón, §4.79). Lleva el bloque `# --- INICIO lib/salida.sh … FIN ---` que el
`Makefile` (tarea 6) sustituye por el fichero: `make verificador`. Probado:
la copia empaquetada, suelta en `/tmp`, carga el vocabulario (`--forma x` da el
`[FALLO]` del guion, `rc=2`); la **sin** empaquetar, suelta, se niega y lo dice
(`rc=2`). Y una trampa: **el `awk` de macOS no tiene `strftime`**; el primer
empaquetado murió en la línea 83 y dejó un fichero de 82 líneas que «pasaba»
la prueba por casualidad, porque la comprobación de `--forma` va antes.

La columna: `[OK] [FALLO] [AVISO] [OMIT] [DATO] [OJOS]` rellenadas a ocho;
`verificar-instalacion.sh` tenía nueve y ahora es la misma que
`verificar-branding.sh` **por construcción**: llaman a la misma función.

#### (d) Tarea 4, `MEDICIONES.md` partido en `mediciones/`

19 399 líneas, **82 cortes**: 79 `### 4.NN`, `## 4.`, `## 9.` y `### A3`. Los
`###` sin número (línea 180, «Las dos barreras…») **no cortan**: van dentro de
la §4.1. El orden original **no era numérico** (la §9 y la A3 en medio de la
§4, la §4.22 detrás de la §4.24) y se conserva en `mediciones/.orden.tsv`. La
cabecera (1-80: prosa, «Cómo leer esto» y la tabla de vigencia) va a
`mediciones/LEEME.md` verbatim más el índice; `MEDICIONES.md` queda de puntero.

**El control:** concatenar los 82 con el título devuelto a su nivel y sin el
`../` → `diff` contra el original: **0 líneas**. Negativo: quitar la línea 5 del
4.37 y el `diff` la ve (`8525d8524`). Lo único que cambia: 82 títulos de nivel y
**cuatro** enlaces relativos con `../` delante. Un quinto —`4.66:17396`,
el enlace a `alojamiento.md` sin ruta— estaba **dentro de un bloque de código**: es
la cita del enlace roto de §4.66g, no un enlace, y el partidor no distinguía
vallas; restaurado a mano.

`enlaces.sh` indexa `mediciones/*.md` como documento «MEDICIONES.md» y entiende
`# 4.NN`. **Trampa:** hasta hacer `git add`, `mediciones/` no estaba en el corpus
(`git ls-files`) y la medición dio 1 506 referencias en vez de 2 426, con un
`[OK]` falso de «ya no cita `§6.8`». Con el índice al día: 2 440 (2 426 + 14 de
los textos de la tarea), 9/0. La rota declarada `§6.8` pasa a **dos** claves (4.2 y
4.66) porque el fichero único las juntaba. `grep '§4.37'`: 88 en `794f57d`, 91
ahora, y las 88 están (el `diff` de reconstrucción está vacío).

#### (e) Tarea 5, la tabla de vigencia entera y `bancos/vigencia.sh`

La tabla tenía **32** filas `§4.x`, no 33 como contaba la casilla. Faltaban
**47 y no 46**: las 46 de los rangos listados **más la §4.45**, que ningún
rango nombraba —la encontró el banco en su primera pasada—; y una 48.ª, la
introducción de la §4 (`4-estado-del-arte.md`), que desde la tarea 4 es un
fichero y el banco exige una fila por fichero. Las 46 se redactaron leyendo
cada sección y sus citas posteriores (borrador de un agente de sólo lectura con
los rangos de líneas leídos por fila, auditado); el criterio de la tercera
columna está escrito en la cabecera del bloque.

`bancos/vigencia.sh`: secciones = primera línea de cada `mediciones/*.md`; filas
= las `| §4.NN |` **dentro** de la tabla. Tres fallos posibles —sin fila, sin
sección, repetida—. Control delante: quitar la fila de la **primera** sección
(no se elige a mano) y colar `| §4.999 |` tras la cabecera; los dos rojos **por
su motivo**; el comparador del control corre en subshell para no sumar.

Dos trampas del propio banco, vistas al estrenarlo: **bajo `LC_ALL=C` «`§?`»
hace opcional sólo el SEGUNDO byte del signo** (son dos), y `| A3 |` no casaba
—«§A3 existe y NO tiene fila» con la fila delante—; arreglado con `(§)?`. Y el
primer control descontaba «un» fallo a mano cuando el comparador daba tres.
Resultado: **82 secciones, 82 filas, 3/0** (83 y 83 desde que existe ésta).
Negativo sobre una copia sin la fila de §4.45: `[FALLO]` nombrándola.

#### (f) Tarea 6, `Makefile`, bancos en la CI y `shellcheck`

`Makefile` (para el `make` 3.81 de macOS) con la configuración en `mk/bancos.mk`,
`mk/paquetes.mk` y `mk/medio.mk`, la forma de `pop-os/iso`: `bancos`,
`bancos-medios`, `banco-autosuficiencia`, `paquetes`, `iso`, `dos-veces`,
`medios/SHA256SUMS`, `qemu`, `verificador`.

**`shellcheck` no está en el Mac y no hace falta:** `bancos/shellcheck.sh` usa el
binario si lo hay y si no `koalaman/shellcheck:stable` por docker (0.11.0).
Sobre los guiones versionados: **0 error, 6 warning, 116 note, 0 style**. Los
seis se **justificaron en su sitio** con `# shellcheck disable=SCxxxx  # motivo`
—SC2046 en `enlaces.sh` (división querida), SC2034 ×3 (columnas de TSV que no
se usan), SC2010 ×2 (`ls | grep` en guiones de VM que no se reescriben sin
ejecutarlos)— y el banco exige motivo a **toda** directiva: cazó tres mudas que
ya había. Puerta: error y warning bloquean; note/style se cuentan y se dicen.
Control: un guion de mentira con `ls | grep` sale SC2010 (rojo) y uno limpio
pasa.

**Hallazgo: el heredoc de `crear-ci.sh` (entonces `06-ci.sh`) llevaba cinco
días distinto de `build.yml`** —33 líneas: la tarea 13 puso la matriz de dos
arquitecturas en uno y no en el otro, pese al aviso de la cabecera—.
`bancos/ci-copias.sh` lo vigila (control: un byte cambiado se ve); rojo la
primera vez, sincronizado **copiando** el fichero dentro, verde.

`make bancos` en el Mac: ocho bancos en verde (con `versiones`, de la tarea 14).
**El sabotaje:** `banco-cadena.sh` con una espera cambiada a
`SABOTEADO.squashfs` → `make bancos` `rc=2`, «`[FALLO] lowerdir= … esperaba
…/SABOTEADO.squashfs`», «`make: *** [banco-cadena] Error 1`»: rojo **por ese
motivo**; restaurado byte a byte. Trampa 60 otra vez, con `make`: «`make bancos
> fichero`» pasa por el hook de `rtk`, que recorta el fichero a 51 líneas y deja
«`... (71 lines truncated)`» **dentro del fichero**; `/usr/bin/make`.

`medios/SHA256SUMS`: las cinco sumas calculadas son las cinco escritas a mano
(listas ordenadas, `diff` vacío); el objetivo las genera. `make verificador`:
861 líneas, `bash -n`, sin `source`.

**La CI, simulada tres veces en un `ubuntu:24.04` limpio por docker** (`mawk`,
`shellcheck` 0.9.0, `ffmpeg`), y las dos primeras cazaron cosas reales: (1)
`enlaces.sh` daba cinco `[FALLO]` por rutas `medios/*.sh`, que en un clon no
existen porque `medios/` no se versiona → regla nueva «MEDIOS, no versionado»,
contada como `[OMIT]`; (2) `shellcheck` 0.9.0 cazó un SC2046 en el propio
`bancos/shellcheck.sh` recién versionado, y el banco **se denunció a sí mismo**
dos veces por buscar «`shellcheck disable=`» en su prosa y en su propio `[OK]`:
ahora sólo cuentan las líneas que **son** directiva. La tercera: `make bancos`
`rc=0`, los siete de entonces en verde. **`[OMIT]`: la CI de verdad (un push)
no se ha lanzado**; romper la CI a propósito exige un push y eso es de Jorge.

#### (g) Tarea 7, vaciar `ENCINA-OS.md` §7

§7 medía **1 772 líneas** (400-2171). El cuerpo (404-2171, 1 768 líneas) va
**verbatim** a
[tareas/cerradas/empieza-aqui-2026-08-08-a-2026-08-25.md](../tareas/cerradas/empieza-aqui-2026-08-08-a-2026-08-25.md),
con cabecera de 16 líneas y **seis** enlaces relativos con `../../` delante
(fuera de vallas). Control: el fichero sin cabecera y sin `../../` es byte a
byte el cuerpo (`diff` 0); negativo: quitar una línea se ve (`100d99`). El §7
nuevo son **38 líneas** (< 60): la fase 2 en curso, cómo se cierra, la fase 3
y el puntero. `ENCINA-OS.md` pasa de 2 651 a 917 líneas. La rota declarada
(`ENCINA-OS.md`, §6.1) vivía en §7 y ahora está en el archivo: clave movida;
`enlaces.sh` 9/0/5/3.

#### (h) Tarea 15, el constructor versionado en `docker/`

`docker/Dockerfile.constructor` (`ubuntu:24.04`, las siete herramientas de
`preparar-entorno.sh` más `build-essential`, `gnupg` y `ca-certificates`,
`LC_ALL=C`, `USER ubuntu`, `/work`) y `docker/construir-paquetes.sh`: 0) las
herramientas, o `morir` **nombrando** la que falta; 1) `git archive HEAD` a
`/work`; 2) los tres guiones con `ENCINA_REPO=/work`; 3) `comprobar-propios.sh`
contra el manifiesto; 4) a `/salida` **sólo si cuadran**. La puerta es
`make paquetes` (`mk/paquetes.mk`; en Linux, los guiones directos).

Tres pasadas hasta que salió, y las tres dicen algo: (1) **los guiones se
niegan como root** (`requiere_no_root`) → `USER ubuntu`; (2) `rm -rf /work`
«Permission denied» (`/` es de root) → se vacía por dentro; **y el `grep
'^\[OK\]'` de `comprobar-propios.sh` no veía el `[OK]` con dos espacios de
sangría desde la tarea 3** → `'^ *\['`; (3) **verde**: commit `03216f8`, 361
ficheros, 38/39/14 comprobaciones, y

```
6929df0e69ac1c31…  encina-branding_0.1.17_all.deb        <- las huellas del manifiesto, las tres
640f508e3802a251…  encina-firefox-native_0.2.1_all.deb
204081f0ff3c5dc3…  encina-meta_0.2.1_all.deb
Ubuntu 24.04.4 LTS arm64   dpkg-dev 1.22.6ubuntu6.6   lintian 2.117.0ubuntu1.5
```

Es la primera vez que esas huellas salen de una máquina que no es `encina-dev`
ni el runner. **Control:** la receta sin `lintian` → «`[FALLO] a la receta le
falta: lintian`», `rc=1`, **antes** de construir; sin `rsvg-convert` → «`le
falta: rsvg-convert`». `construir-todo.sh` **no se toca** (el alcance son los
`.deb`). Y un hallazgo colateral que la tarea 11 usa: el mismo contenedor genera
el índice `Packages` (`dpkg-scanpackages`), que era lo otro que macOS no podía.

#### (i) Tarea 9, `TRAMPAS.md`

**68 filas**: 1-61, 26bis, y **28 a 33 por duplicado**. La casilla decía «45
números, llegan hasta la 58, trece huecos»; contadas las cabeceras `**N. …**` de
`SCRIPTS.md` son **61 sin ningún hueco de definición**, pero el bloque del
2026-08-17 («Y cuatro más…») contó a partir de la 27 pisando 28-33 del
2026-08-12/13. A cuál apunta cada «trampa N» externa, comprobado cita a cita:
28 → siempre la b, 29-31 → siempre la a, 32 → las dos, 33 → nadie. La
numeración **no se cambia**; qué número se queda cada «b» es de Jorge. Columnas:
número, síntoma, causa, **a qué aplica** (la que no existía), dónde está medida
(`SCRIPTS.md:línea` enlazado al fichero, y `§4.NN`). Las `MEDICIONES.md:línea`
del fichero único se quitan —tras la tarea 4 no significan nada—; las `§`
deducidas por fecha van marcadas y no se dan por buenas. Lo sin número (58 filas
de §9) se queda donde está; las numeraciones **locales** que chocan con la
global (`enlaces.sh` 1-7, `inventario-marca.sh` 1-4) se avisan. `enlaces.sh`
sobre las 68 filas: 9/0.

#### (j) Tareas 8, 14 y 16

**Tarea 8.** `diario.sh "qué se midió" "qué salió mal" "qué toca mañana"` →
entrada de cuatro líneas (fecha y tres sub-viñetas), verificada con la trampa 13
antes de hacer el commit. Probado en un clon desechable: la línea más larga de
la entrada nueva, **59 caracteres** (< 1 500); la vieja más larga sigue en 6 518
y no se reescribe. Un argumento: como antes, con `[AVISO]`; cuatro: `morir`.

**Tarea 14.** La fuente de cada versión es el `debian/changelog` de su `.deb`,
que las filas `PROPIO` de `imagen/repo-manifiesto.tsv` tienen que repetir; **la
versión del producto es la de `encina-meta`** (`fabricar-iso.sh` la compone en
el `Volume id`, §4.57f); el `24.04.4 LTS "Nutria Nocturna"` de `disk-info` es
de la base (33b). Escrito en `AGENTS.md` §3. `fabricar-iso.sh` y
`fabricar-seed.sh` leen los cuatro nombres del manifiesto en vez de llevarlos
escritos en dos sitios; control: los cuatro que salen son byte a byte los de
antes, y la huella de la ISO la coteja (k). `bancos/versiones.sh`: (A) manifiesto
== changelog ×3, más `autofirma` contra el hermano; (B) 23 documentos **vivos**
sin ninguna versión que no sea la del manifiesto, salvo tachada, en valla de
código, o en línea con «histór». Tres versiones del banco hasta que midió, cada
una cazada por su negativo o por el recuento: el control esperaba la cita en la
línea 2 y estaba en la 1; **eximía lo que va entre acentos graves y así eximía
los nombres de paquete —0 fallos sobre 24 citas rancias—**; y «`histor`» no casa
con «`histórica`» bajo `LC_ALL=C` (la `ó` son dos bytes). De las 24 citas
rancias de los documentos vivos, **tres afirmaban el estado de hoy**
(`AGENTS.md:16` —la línea de alcance—, `AGENTS.md:78`, `ENCINA-OS.md:245`) y van
tachadas con enmienda; 21 son hechos de aquel día y llevan la marca. Resultado
7/0/0/1; negativos sobre el árbol real: un dígito en `AGENTS.md:16`
(`+encina9`) y `encina-meta 0.2.2` en el manifiesto, los dos `[FALLO]` por su
motivo, restaurados byte a byte.

**Tarea 16.** [tareas/ojos.md](../tareas/ojos.md): **47 filas** —15 `amd64`
(el Acer), 12 `arm64` sobre `63f360dd…`, 20 de historial—, con la frase literal
de la casilla, el enlace, el medio, la arquitectura y el estado; dos reglas
escritas (ninguna fila la marca un agente; las casillas no se reescriben). Lo
que salió al juntarlas: dos casillas `[x]` de `AGENTS.md` (1875-1897) dicen
«mirado en pantalla» **sin nombrar a Jorge** (§4.34c y §4.35h son capturas del
agente) y van como «MIRADO POR EL AGENTE, NO POR JORGE»; la casilla que decide de
E1 (`AGENTS.md:1003`) tampoco nombra quién; las filas a y d de la receta del
hierro siguen «sin desglosar»; la firma en `valide.redsara.es` **nunca se ha
hecho en `amd64` ni sobre `63f360dd…`**; y la huella del `amd64` que instaló el
Acer el 23 no está escrita en §4.70 (`8924f148…` es deducción).

#### (k) Tarea 10, renombrar por verbo y paquete

La convención es la de `encina-autofirma` —verbo primero, sin números—:
`preparar-entorno`, `colocar-esqueleto`, `generar-activos`,
`construir/instalar/verificar-{branding,firefox,meta}`, `crear-ci`. Trece
`git mv`, y la sustitución en **todo lo versionado salvo** los registros
(`mediciones/`, `DIARIO.md`, `tareas/cerradas/`: 71 citas conservadas) y los
ficheros que son bytes del producto (`debian/changelog` 8, `rules` 1,
`encina.xml` 2, `imagen/encina-seed.sh` 1). Dos pasadas: la primera (102 citas)
se dejó las que llevaban **ruta delante** porque la expresión excluía la barra,
y se vio porque la lista de «quedan nombres viejos» no salía vacía; la segunda,
103 más. `SCRIPTS.md` lleva la tabla de equivalencias como la del 2026-08-13;
`enlaces.sh` tiene los trece nombres viejos y `scripts/construir-deb.sh` (la
propuesta que no fue) en su exclusión, 9/0; `build.yml` y el heredoc, `mk/`,
`docker/`, `construir-todo.sh` y `lib/vm.sh` apuntan a los nuevos. **El control
funcional:** `make paquetes` en el contenedor con los tres `construir-*.sh`
renombrados → 38/39/14 comprobaciones y **las tres huellas del manifiesto**.
Los `README.md` de `debian-packages/` se tocaron y la huella demuestra que no
viajan en el `.deb`.

#### (l) Tarea 11, `fabricar-iso.sh` una función por fase — y la huella

**La línea base, antes de tocar el guion:** un repositorio cosechado con los
28 `.deb` de `medios/repo-trabajo` (todos menos `encina-branding` 0.1.15) más
`debian-packages/encina-branding_0.1.17_all.deb`, los 29 contra el manifiesto
(**0 no cuadran**), el índice `Packages` por `dpkg-scanpackages` **en el
contenedor de la tarea 15** (29 entradas, `2f69fcff…`), y `fabricar-iso.sh`
**del árbol `794f57d`** (`git archive`, antes de tocar nada) sobre la ISO
oficial `arm64`:

```
sha256: 63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5   <- LA HUELLA VIGENTE de §4.79, en 47 s
```

Sin VM: la reproducibilidad del medio `arm64` ya no depende de `encina-dev`
(`construir-todo.sh` sigue siendo la orden del producto; esto es el control).

**Lo que se decidió, que la casilla decía que era trabajo y no transcripción:**
las fases son las **catorce numeradas 0-13** —la 8, la 9 y la 12 sólo tenían su
`echo "== N."` y hoy tienen su marca—; la 5 se parte en **siete subfases**
(`5`, `5a`, `5b`, `5b-bis`, `5c`, `5c-bis`, `5d`, `5e`: cada una toca **un**
fichero del medio y tiene su control), y la `10bis` es la excepción del
`eltorito.img` que la 10 declara. **22 funciones** `fase_N_nombre()`, y al final
la lista que las llama en orden. Las banderas `--sin-*` no se dispersan: cada
subfase lee la suya, como antes. Las variables siguen globales a propósito y
**los tres `declare -a` salen de las funciones** (dentro harían la variable
local y las fases siguientes no la verían). `bash -n` pasa;
`--leer-mecanismos` da `1 1 1 1` como antes; y el `diff` de forma —quitando
`fase_x() {`, `}`, las tres marcas y la lista— contra el guion de antes enseña
sólo los `}` de las funciones que ya existían y dos líneas en blanco.

**El control, que es el único que vale aquí:** dos pasadas del guion por
fases —con todo lo de las tareas 2, 3 y 14 dentro— sobre el mismo repositorio
cosechado y la misma ISO oficial que la línea base:

```
pasada 1   63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5   rc=0   capa=SI volid=SI info=SI menu=SI
pasada 2   63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5   rc=0
cmp pasada1 pasada2         : identicas byte a byte
cmp pasada1 linea-base      : identica byte a byte
linea base (794f57d)        : 63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5   <- §4.79
```

**La misma huella que antes del cambio, apuntada antes de tocar nada, y las
dos pasadas siguen dando la misma.** Y una consecuencia que no estaba
prevista: la casilla y `ENCINA-OS.md` §7 decían que las huellas vigentes
*caducarían a propósito* con esta fase. **La `arm64` no ha caducado**:
`63f360dd…` sale igual del árbol refactorizado. Lo deducido queda al lado de lo
medido —la de `amd64` (`3d5d12a9…`) no se ha refabricado hoy y no se afirma
nada de ella hasta la fase 3—.

#### (m) El cierre: los bancos sobre el árbol entero movido

Con las dieciséis casillas marcadas, el bloque —`refactorizacion.md` y
`organizacion-comparada.md`— movido a `tareas/cerradas/` y sus enlaces
redirigidos (siete dentro, veinticinco fuera; cuatro en registros que
apuntaban al bloque, arreglados como los dos de §4.66), la red entera sobre
el árbol final:

```
make bancos                                    rc=0
  bancos/enlaces.sh          correctas: 9   fallos: 0   avisos: 5   omitidas: 3   (era 9/0/5/2: +1 omitida, las rutas bajo medios/)
  bancos/vigencia.sh         correctas: 3   fallos: 0                              (83 secciones, 83 filas)
  bancos/ci-copias.sh        correctas: 2   fallos: 0
  bancos/versiones.sh        correctas: 7   fallos: 0   avisos: 0   omitidas: 1
  imagen/banco-cadena.sh     correctas: 8   fallos: 0                              (= linea base)
  scripts/banco-veredicto.sh correctas: 9   fallos: 0                              (= linea base)
  veredicto-conteo.py        correctas: 8   fallos: 0                              (= linea base)
  bancos/shellcheck.sh       correctas: 4   fallos: 0   avisos: 1                  (43 guiones: 0 error, 0 warning)
make bancos-medios           correctas: 1   fallos: 0   omitidas: 5                (= linea base)
fabricar-iso.sh --leer-mecanismos medios/encina-os-arm64.iso   -> 1 1 1 1          (= linea base)
comprobar-propios.sh encina-branding  -> [OK] 6929df0e69ac…                        (= linea base)
```

**`bancos/enlaces.sh` en verde sobre el árbol ya movido: es la condición de
cierre de la tarea 1 y de la fase entera**, y se cumple con 2 400 y pico
referencias, 43 guiones versionados y 82 ficheros de mediciones más éste. Y la
otra condición —el documento de la tarea 12 sin ninguna fila sin veredicto—
estaba hecha desde el 2026-08-23. **La fase 2 está completa.** Lo siguiente es
la fase 3: refabricar los dos medios con `make dos-veces` desde estos guiones,
instalarlos, mirarlos, y publicar.

#### (n) Lo que esta fase NO ha hecho, y no se da por bueno

- **`[OMIT]` ejecutar los guiones de VM** (`scripts/*` salvo los bancos,
  `verificar-instalacion.sh`, `banco-autosuficiencia.sh`): `encina-dev` no se
  encendió. Su garantía es de forma —`bash -n`, `shellcheck`, el control de
  funciones y opciones de (c)— y, para los tres de construcción, de ejecución
  **en el contenedor** (h) y (k).
- **`[OMIT]` la CI de verdad**: ningún push. Los tres jobs simulados por docker
  son el sustituto, y el sabotaje está pagado en local.
- **`[OMIT]` el medio `amd64`**: la línea base y el control de (l) son `arm64`.
  El `amd64` se refabrica en la fase 3 con `make dos-veces ARQ=amd64`.
- **`[OMIT]` las 20 guardas mudas** de §4.67d, y `banco-mecanismos.sh` sigue
  `[OMIT]` en cinco de seis medios que ya no están con esos nombres.
- Las dos huellas vigentes **caducan igualmente con la fase 3**: la que se
  publique se fabrica entonces, con estos guiones.
