# 4.81 LA SESIÓN PREVIA A LA FASE 3: los dos medios refabricados desde los guiones definitivos —`63f360dd…` y `3d5d12a9…` otra vez, dos pasadas cada uno—, el archivo de Ubuntu ya no sirve el producto anclado (openjdk-17 retirado, Launchpad sí), la propuesta para las trampas 28-33, y la CI de verdad (2026-08-28)

**Qué es esto.** Las tres cosas que §4.80 (m) y (n) dejaron `[OMIT]` al
cerrar la fase 2, hechas de seguido la tarde del 2026-08-28 sobre `main` en
`464b4fd`, árbol limpio: (a) la CI de verdad, (b) qué número se queda cada
trampa «b» de las 28-33 —**propuesta, no decisión: la decisión es de Jorge y
aquí no se renumera nada**—, y (c) `make dos-veces` en las dos arquitecturas.
**No es la fase 3**: no se instala ni se arranca ningún medio, y no se
publica nada. Los bancos antes y después están en (d).

`make bancos` antes de tocar nada, sobre `464b4fd`: `rc=0`, ocho en verde con
los recuentos de §4.80m (9/0/5/3 · 3/0 · 2/0 · 7/0/0/1 · 8/0 · 9/0 · 8/0 ·
4/0/1).

#### (a) La CI de verdad

**`[OMIT]` mientras no haya push.** `HEAD` es `464b4fd` y `origin/main` sigue
en `794f57d` toda la sesión (medido con `/usr/bin/git fetch` cada minuto desde
las 15:00; la última ejecución de GitHub es la `32850299915`, sobre `794f57d`,
`success`, del 2026-08-25 —la de antes de la fase 2, sin el job «bancos»—).
El push es de Jorge. Lo que hay que hacer cuando esté, y no se ha hecho:

```
gh run list --limit 3            # la ejecución sobre 464b4fd (o el commit del push)
gh run view <id>                 # los siete jobs: «bancos» y las seis filas de build
gh run view <id> --log --job <id del job bancos>
```

y compararlo con la simulación de §4.80f (`ubuntu:24.04`, `mawk`, `shellcheck`
0.9.0, `ffmpeg`) y con el `make bancos` de aquí arriba: los mismos ocho bancos,
los mismos recuentos. Lo que el runner diga y la simulación no dijo es un
hallazgo y va aquí con su salida literal.

#### (b) Las trampas 28-33 duplicadas: la propuesta, medida cita a cita, y NADA renumerado

**Lo que se cuenta y cómo.** Sobre los 374 ficheros de `git ls-files` en
`464b4fd`, cada aparición de «trampa(s) N» con N de 28 a 33 —y las que llevan
letra, «28b … 33b»—, leída una a una con su contexto para decidir a cuál de las
dos apunta. Se descartan las coincidencias de número («las 28 huellas», «las 32
líneas de `cdimage`», «las 28 salen en español»: 13 líneas). El instrumento:

```
/usr/bin/git ls-files | while read f; do
  /usr/bin/grep -n -o -E '.{0,40}(trampas? |la |las |y )(2[89]|3[0-3])(a|b)?([^0-9]|$).{0,40}' "$f" | sed "s|^|$f:|"
done
```

**A cuál apunta cada cita**, y coincide con lo que la cabecera de `TRAMPAS.md`
decía el 2026-08-28 (28 → b, 29-31 → a, 32 → las dos, 33 → nadie por número):

| Nº | Apuntan a la **a** | Apuntan a la **b** |
|---|---|---|
| 28 | — | `DIARIO.md:63` · `SCRIPTS.md:2693`, `:2822` · §4.61 (líneas 180 y 639) · `scripts/contar-arranques.sh:23`, `:96` · `scripts/fabricar-vm-medio.py:263` — **8** |
| 29 | `DIARIO.md:80` · `ENCINA-OS.md:602` · §4.34:33 · §4.71:18 — **4** | — |
| 30 | §4.72:40 — **1** | `TRAMPAS.md:111` («la 30b», con letra) — **1** |
| 31 | `SCRIPTS.md:2006` («el arreglo evidente de la 31», dentro de la 32a) · `SCRIPTS.md:3102` — **2** | `TRAMPAS.md:125` («la 31b», con letra) — **1** |
| 32 | §4.35:394 («la regla de la trampa 32») — **1** | `CLAUDE.md:80` · `SCRIPTS.md:2513`, `:2969` · §4.54:261 · §4.55:117 · `mediciones/LEEME.md:98` · `mk/medio.mk:44` · `scripts/fabricar-vm-medio.py:22`, `:27`, `:48` · `tareas/cerradas/organizacion-comparada.md:184` — **11** |
| 33 | — | `AGENTS.md:150` y `bancos/versiones.sh:15` («33b», con letra, escritas el 2026-08-28) — **2** |

Las dos que no se leen solas: `SCRIPTS.md:2513` («**no era la trampa 32:** el
`debug.log` crecía») y §4.55:117 («ni colgada ni la trampa 32», con el
`debug.log` creciendo) hablan del síntoma de la **b** —`debug.log` congelado por
`Drive.Identifier` repetido—, no de la regla de la a. `SCRIPTS.md:2969` (la 49:
«es la trampa 32 en una forma nueva») es la b: identificadores de unidad.

**En total: 8 a la a, 24 a la b**; de las 24, **6 están en registros** que no
se reescriben (`DIARIO.md`, §4.54, §4.55, §4.61 ×2, `organizacion-comparada.md`)
y **18 en documentos vivos y guiones**. Además, las citas «meta» a la propia
duplicación (§4.80i, `DIARIO.md:95` y `:97`, la cabecera de `TRAMPAS.md` y
`refactorizacion.md:419`), que hablan del hecho y no de una trampa.

**Las dos opciones, con su precio:**

- **(a) Dejar 28a/28b … 33a/33b para siempre.** Precio hoy: cero ediciones.
  Precio permanente: toda cita nueva a una de las doce tiene que llevar la letra
  o es ambigua; las seis filas «b» llevan *(número duplicado)* de por vida; y la
  regla de lectura de las citas viejas («28 → b, 29-31 → a, 32 → las dos») vive
  sólo en la cabecera de `TRAMPAS.md`.
- **(b) Dar a las «b» los números 62-67** (28b→62, 29b→63, 30b→64, 31b→65,
  32b→66, 33b→67; el último definido en `SCRIPTS.md` es el 61 y el pie de
  `TRAMPAS.md` ya dice «la siguiente es la 62»). Precio, contado: **6 cabeceras**
  de `SCRIPTS.md` (líneas 2298, 2313, 2322, 2329, 2335, 2344 hoy) · **6 filas y
  la cabecera** de `TRAMPAS.md` · **18 citas vivas** —28→62: `SCRIPTS.md` ×2,
  `contar-arranques.sh` ×2, `fabricar-vm-medio.py` ×1; 30b→64 y 31b→65:
  `TRAMPAS.md` ×2; 32→66: `CLAUDE.md`, `SCRIPTS.md` ×2, `LEEME.md`, `mk/medio.mk`,
  `fabricar-vm-medio.py` ×3; 33b→67: `AGENTS.md`, `versiones.sh`— · y **6 citas
  en registros que NO se tocan**: la nota fechada de `TRAMPAS.md` dice cómo
  leerlas («trampa 28» o «32» en un registro anterior a la renumeración = 62 o 66
  cuando habla de `utmctl` o de `Drive.Identifier`). Lo que se pierde: el orden
  cronológico —62-67 serían del 2026-08-17 y las 34-61 de después— y las seis
  quedan lejos de su bloque en `SCRIPTS.md` (el texto largo no se mueve; sólo
  cambia el número de la cabecera). Se aplica con `/usr/bin/sed` línea a línea
  sobre los 13 ficheros vivos, y la comprobación es `enlaces.sh` más el
  recuento de arriba repetido: 0 citas a «28b … 33b» y 0 a «trampa 28/32» fuera
  de registros.

**Mi recomendación, por si sirve: (b).** El precio se paga una vez; el de (a)
se paga en cada cita que se escriba a partir de ahora.

**Y un hallazgo que va con cualquiera de las dos, y que sí se ha corregido
hoy porque no depende de la elección:** la columna «dónde está medida» de
`TRAMPAS.md` cita `SCRIPTS.md:NNN` «la línea del texto largo el 2026-08-28», y
**ese mismo día, después, la tarea 10 insertó 66 líneas en `SCRIPTS.md`** (la
tabla de equivalencias de los nombres) por delante de todas las trampas: las
**68 filas** apuntaban 66 líneas por encima de su cabecera, y las citas
«citada en `SCRIPTS.md:2928`/`:2447`/`:2932`» y los rangos en prosa también.
Medido con las cabeceras `**N. …**` de la sección de trampas —y no con la
lista 1-6 de «El laboratorio de E2», que casa con el mismo patrón y despista—:

```
1    cita 533   hoy 599   +66      28a  cita 1829  hoy 1895  +66      28b  cita 2232  hoy 2298  +66
…    las 68 filas: desfase +66, sin excepción (trampa 1 en 794f57d: línea 533; en 464b4fd: 599)
```

Corregido sumando 66 a cada `SCRIPTS.md:NNN` y a cada rango de `TRAMPAS.md`
(la orden está en su nota fechada), y el mismo `awk` da después **desfase 0 en
las 68**. Un número de línea en un documento que se edita a diario caduca con
cada edición y no hay ancla de markdown para una línea en negrita: la nota de
`TRAMPAS.md` dice desde hoy que esa columna caduca y cómo volver a medirla.

#### (c) Refabricar los dos medios desde los guiones definitivos — y el archivo ya no sirve el producto

**Antes de nada, la VM.** `encina-dev` era la única VM que iba a encenderse
(las cinco en `stopped`); `utmctl start` hizo la trampa 28b al pie de la
letra —«`Error from event: The operation couldn't be completed. (OSStatus
error -1712.)`», **`rc=0`**, y `utmctl status` seguía en `stopped` a los 60 s—
con UTM vivo desde hacía 2 h 25 min y contestando a `utmctl list`. El remedio
escrito (28b): reiniciar UTM, respaldando antes `com.utmapp.UTM.plist`
(11 517 bytes, `44cad3d8…`) y comprobando las dos mitades (trampa 18):
`utmctl list` antes y después, **5 VMs y `diff` vacío**. Al segundo
`utmctl start`, `started` a los 2 s y `ssh` a los ~12 s:

```
encina-dev  1ee16aeb6e284f668fde407cfa31a3ac  Ubuntu 24.04.4 LTS  arm64  up 0 minutes
encendidas: 1
```

Todo lo que sigue, bajo `caffeinate -dimsu` (33a). Al terminar, `utmctl stop`
y el estado esperado: `stopped` a los 2 s, encendidas: 0.

**La huella previa, apuntada antes de tocar el medio**, y el medio apartado con
nombre de punto para que el `mv` final de la receta no pise el vigente si la
huella saliera otra:

```
-rw-r--r--@ 2 jorge  staff  3721265152 Aug 25 12:46 medios/encina-os-arm64.iso
63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5  medios/encina-os-arm64.iso
apartado a medios/.previa-arm64-63f360dd.iso
```

**`make dos-veces ARQ=arm64`, tal cual, sobre `464b4fd` con el árbol limpio:
muere en la cosecha de la pasada 1.** Los pasos 0-3 en verde —374 ficheros a
los dos lados, los tres `.deb` propios construidos en `encina-dev` con las
huellas del manifiesto (`encina-branding_0.1.17` `6929df0e…`,
`encina-firefox-native_0.2.1` `640f508e…`, `encina-meta_0.2.1` `204081f0…`)—
y después:

```
=== 4. la cosecha -- 24 de fuera + autofirma, POR HUELLA ===
        (la 1a orden SALE INCOMPLETA a proposito: falta autofirma. Su [FALLO] es el control)
  [FALLO] el repositorio NO esta completo: 0 no cuadran, 3 ausentes
        == 7. el directorio contra el manifiesto, entero
                ausente     openjdk-17-jre_17.0.19+10-1~24.04.2_arm64.deb
                ausente     openjdk-17-jre-headless_17.0.19+10-1~24.04.2_arm64.deb
                ausente     autofirma_1.9.1+encina4_all.deb
                cuadran 26 de 29   no cuadran 0   ausentes 3
                .deb en el directorio: 26
  [FALLO] CONTROL ROTO: sin autofirma esperaba 28 .deb y hay 26
make: *** [dos-veces] Error 1
```

(El registro de esa orden dice «rc de make dos-veces: 0» una línea más abajo:
es un `echo` mío entre `make` y `$?`, no `make`, que abortó con `Error 1` y no
hizo pasada 2.) El tercer ausente es el esperado —autofirma llega en la 2.ª
orden—; los otros dos no. `cosechar-repo.sh` solo, con su salida entera
(`construir-todo.sh` la recorta a `tail -6`), da la respuesta que sabe dar
desde §4.36 y que **nunca había salido en serio**:

```
        [RETIRADO] openjdk-17-jre 17.0.19+10-1~24.04.2 (arm64) no esta en el archivo
        [RETIRADO] openjdk-17-jre-headless 17.0.19+10-1~24.04.2 (arm64) no esta en el archivo
[HALLAZGO] el archivo de Ubuntu retira las versiones superadas, y esto
           ya no se puede bajar en la version que el manifiesto pide:
             openjdk-17-jre 17.0.19+10-1~24.04.2   (hoy hay: 17.0.11+9-1 17.0.20+8-1~24.04 )
             openjdk-17-jre-headless 17.0.19+10-1~24.04.2   (hoy hay: 17.0.11+9-1 17.0.20+8-1~24.04 )
           NO se coge la version nueva: eso cambiaria lo que el producto
           lleva, y es una decision de producto, no de este guion.
```

Y el `pool` ya no lo tiene, en ninguna de las dos arquitecturas:

```
http://ports.ubuntu.com/ubuntu-ports/pool/universe/o/openjdk-17/openjdk-17-jre_17.0.19+10-1~24.04.2_arm64.deb   HTTP/1.1 404 Not Found
http://archive.ubuntu.com/ubuntu/pool/universe/o/openjdk-17/openjdk-17-jre_17.0.19+10-1~24.04.2_amd64.deb     HTTP/1.1 404 Not Found
```

**Lo que sí lo tiene: Launchpad**, que conserva cada compilación publicada.
Los cuatro (dos por arquitectura), bajados y cotejados contra el manifiesto
**antes** de usarlos para nada:

```
[OK]    openjdk-17-jre_17.0.19+10-1~24.04.2_arm64.deb           b66c2f6851a90ed7…    232714 bytes  = manifiesto
[OK]    openjdk-17-jre-headless_17.0.19+10-1~24.04.2_arm64.deb  05a1e87f5200cc6b…  47517766 bytes  = manifiesto
[OK]    openjdk-17-jre_17.0.19+10-1~24.04.2_amd64.deb           8002b24fd6c9c6e5…    233754 bytes  = manifiesto
[OK]    openjdk-17-jre-headless_17.0.19+10-1~24.04.2_amd64.deb  ff074bca2b1ffa2a…  48090566 bytes  = manifiesto
```

(`https://launchpad.net/ubuntu/+archive/primary/+files/<fichero>` → 303 →
`https://launchpadlibrarian.net/8589567NN/<fichero>` → 200.) Y las cosechas
locales del 22 de agosto —`medios/repo-trabajo` y `repo-trabajo-amd64`— siguen
cuadrando 28 de 29 con su manifiesto (el ausente es `encina-branding_0.1.17`;
allí está la 0.1.15, como decía §4.80l).

**Cómo se ha refabricado entonces, y qué NO es esto.** `cosechar-repo.sh`
acepta un `.deb` de ARCHIVO ya presente en `--salida` **sólo si cuadra por
huella** («`ya estaba`»), y `construir-todo.sh` deja elegir ese directorio con
`--trabajo`. Así que las dos pasadas son **las órdenes de la receta de
`mk/medio.mk` literales más `--trabajo <dir>`**, un directorio distinto por
pasada, sembrado **únicamente** con los dos `.deb` retirados (cotejados otra
vez al sembrarlos); los otros 23 bajan del archivo como siempre, los tres
propios se construyen en `encina-dev` y autofirma entra por huella entre sus
tres casi homónimos. Ningún guion del repositorio tocado; el guion de las dos
pasadas es de sesión (scratchpad) y hace lo que la receta, línea a línea.
**No es `make dos-veces`**: `make dos-veces` no puede terminar hoy en ninguna
arquitectura, y eso es una decisión de la fase 3 (abajo).

**`arm64`: dos pasadas, `63f360dd…` las dos, y byte a byte igual al vigente.**
Pasada 1 de 15:04:43 a 15:07:08 y pasada 2 de 15:07:08 a 15:09:26 (2 min 25 s
y 2 min 18 s; el índice `Packages` sale `2f69fcff…`, el mismo que el del
contenedor en §4.80l; la capa `ef15c522…`, 3 084 288 bytes; 501 entradas de la
oficial y 533 de la nuestra en `md5sum.txt`):

```
        sembrado   openjdk-17-jre_17.0.19+10-1~24.04.2_arm64.deb  b66c2f6851a9…  (= manifiesto)
        sembrado   openjdk-17-jre-headless_17.0.19+10-1~24.04.2_arm64.deb  05a1e87f5200…  (= manifiesto)
        bajados: 23   ya estaban: 2   fallos de descarga: 0
  [OK]    29 .deb cosechados sin tocar ninguna ISO, los que pide el manifiesto
  [OK]    Packages: 29 entradas, 2f69fcff80f18f22…
        marca:  capa=SI volid=SI info=SI menu=SI
pasada 1  63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5
pasada 2  63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5
[OK]    dos pasadas, la misma huella: medios/encina-os-arm64.iso  63f360dd755251d10628e71405235979b5b5e13ebf43d9f7e089ecbc2563a1f5
cmp medios/encina-os-arm64.iso medios/.previa-arm64-63f360dd.iso   -> identicos byte a byte
```

**`amd64`: dos pasadas, `3d5d12a9…` las dos, y byte a byte igual al vigente.**
La huella previa, apuntada antes (`3d5d12a9bda40068…`, 6 849 232 896 bytes,
24 ago) y el medio apartado igual que el otro. Pasada 1 de 15:10:46 a 15:13:39
y pasada 2 de 15:13:39 a 15:16:39 (2 min 53 s y 3 min 0 s; `Packages`
`7c4499e6…`; 1 069 entradas de la oficial y 1 101 de la nuestra):

```
        sembrado   openjdk-17-jre_17.0.19+10-1~24.04.2_amd64.deb  8002b24fd6c9…  (= manifiesto)
        sembrado   openjdk-17-jre-headless_17.0.19+10-1~24.04.2_amd64.deb  ff074bca2b1f…  (= manifiesto)
        bajados: 23   ya estaban: 2   fallos de descarga: 0
  [OK]    29 .deb cosechados sin tocar ninguna ISO, los que pide el manifiesto
  [OK]    Packages: 29 entradas, 7c4499e6bad21a16…
        marca:  capa=SI volid=SI info=SI menu=SI
pasada 1  3d5d12a9bda400685beedabf9c1dd4ddde512e419866174b0bb6da7847f801f0
pasada 2  3d5d12a9bda400685beedabf9c1dd4ddde512e419866174b0bb6da7847f801f0
[OK]    dos pasadas, la misma huella: medios/encina-os-amd64.iso  3d5d12a9bda400685beedabf9c1dd4ddde512e419866174b0bb6da7847f801f0
cmp medios/encina-os-amd64.iso medios/.previa-amd64-3d5d12a9.iso   -> identicos byte a byte
```

**Lo que esto dice y lo que no.** Las dos huellas vigentes **no caducan con
la fase 2** —§4.80n lo daba por hecho para la `amd64`; era una deducción, y
era falsa—: salen otra vez de los guiones definitivos, con los tres `.deb`
construidos hoy en `encina-dev` y la cosecha bajada hoy, salvo los dos `.deb`
que el archivo retiró, que son los mismos bytes por huella. Los dos apartados
se borran después del `cmp` (eran el mismo fichero; el bundle
`encina-entrega-63f360dd` conserva su enlace duro). `medios/SHA256SUMS`
regenerado con `make medios/SHA256SUMS`: **`git diff` vacío**. Y sobre las dos
ISOs nuevas: `fabricar-iso.sh --leer-mecanismos` → `1 1 1 1` las dos;
`make bancos-medios` → `correctas: 1  fallos: 0  omitidas: 5`, **igual que la
línea base de §4.80a, y con la misma salvedad: ese banco sólo nombra medios
históricos que ya no están** (el que corre es la oficial `arm64`, y es la única
que mira). No se ha instalado ni arrancado nada: eso es la fase 3.

**Lo que la fase 3 tiene que decidir antes de publicar, y no se decide aquí:**
la receta pública (`cosechar-repo.sh` contra `ports.ubuntu.com` /
`archive.ubuntu.com`) **ya no reproduce el producto anclado**: openjdk-17
`17.0.19+10-1~24.04.2` está retirado y un tercero que siga `construir-todo.sh`
se para donde se paró `make dos-veces` hoy. Tres salidas, cada una con su
precio: (1) que `cosechar-repo.sh` sepa caer a Launchpad —es un guion de
`imagen/`, hoy vedado, y son ~10 líneas—; (2) subir el manifiesto a
`17.0.20+8-1~24.04` —cambia el producto: huellas nuevas, la ISO se instala y se
mira otra vez (`tareas/ojos.md`)—; (3) publicar los 29 `.deb` del repo junto
con la ISO, que es lo que ya hace la propia ISO (`/encina-repo`). Y un aviso
que sale de aquí: **esto le va a pasar a cualquier otro de los 25 de ARCHIVO**
—`firefox` 153.0.4 de Mozilla el primero— cada vez que su archivo publique
una versión nueva; la única fuente que no retira es Launchpad, y sólo para lo
de Ubuntu.

#### (d) Los bancos después, y lo que queda `[OMIT]`

`make bancos` después de todos los cambios de esta sesión (este fichero con
su fila y su índice, `TRAMPAS.md` con el +66 y la nota, `ENCINA-OS.md` §7,
`medios/LEEME.md`), con el fichero nuevo ya en `git add` —`enlaces.sh` sólo
ve `git ls-files`—:

```
make bancos                                    rc=0
  bancos/enlaces.sh          correctas: 9   fallos: 0   avisos: 5   omitidas: 3
  bancos/vigencia.sh         correctas: 3   fallos: 0                              (84 secciones, 84 filas)
  bancos/ci-copias.sh        correctas: 2   fallos: 0
  bancos/versiones.sh        correctas: 7   fallos: 0   avisos: 0   omitidas: 1
  imagen/banco-cadena.sh     correctas: 8   fallos: 0
  scripts/banco-veredicto.sh correctas: 9   fallos: 0
  veredicto-conteo.py        correctas: 8   fallos: 0
  bancos/shellcheck.sh       correctas: 4   fallos: 0   avisos: 1
diff de los recuentos antes/despues: vacio
```

Sigue `[OMIT]`: la CI de
verdad —(a)—; `make dos-veces` tal cual, que no puede terminar hasta que la
fase 3 decida qué hacer con los retirados; la elección de las 28-33; y todo lo
de §4.80n que no era de esta sesión (las 20 guardas mudas de §4.67d, los
guiones de VM salvo los tres de construcción, que hoy sí han corrido en
`encina-dev` dentro de `construir-todo.sh`).
