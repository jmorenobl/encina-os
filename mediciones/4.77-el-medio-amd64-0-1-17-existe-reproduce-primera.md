# 4.77 EL MEDIO `amd64` CON 0.1.17 EXISTE Y SE REPRODUCE A LA PRIMERA: `3d5d12a9…`, dos pasadas byte a byte del mismo commit — y el hook de `rtk` también filtra `diff` (2026-08-24)

**Qué.** Jorge pidió el medio con el 0.1.17 para probar en el hierro todo
junto: el desvío de la bellota (§4.76) y lo pendiente de la fila g. Es la
misma operación de §4.74 con el `.deb` nuevo, y esta vez el GUID fijo de GPT
ya estaba puesto: la reproducibilidad salió **a la primera**.

#### (a) El ritual de los seis sitios, con su control previo

Para `encina-branding 0.1.17` (`6929df0e…`, 6 952 548 bytes): los dos
manifiestos, `H_BRANDING` y el nombre en `encina-seed.sh` (con el comentario
CUARTA fechado), los arrays `FICHEROS` de `fabricar-iso.sh` y
`fabricar-seed.sh`, y los dos `autoinstall*.yaml` regenerados desde el seed.
**El control previo, pagado antes de tocar nada:** el base64 vigente de los
dos yaml decodificaba **byte a byte** al `encina-seed.sh` de HEAD
(`2dcfa9be…`, 31 819 bytes), así que regenerar es la misma operación y no
otra. Y tras regenerar, la verificación simétrica: lo escrito decodifica al
seed nuevo. Único resto de 0.1.16 en `imagen/`: el comentario histórico
TERCERA, fechado, que es lo que debe quedar. Commit `676c79a`.

#### (b) Dos pasadas, una huella, y los tres controles

```
pasada 1  (commit 676c79a)  3d5d12a9bda400685beedabf9c1dd4ddde512e419866174b0bb6da7847f801f0
pasada 2  (el mismo commit) 3d5d12a9bda400685beedabf9c1dd4ddde512e419866174b0bb6da7847f801f0
tam                         6 849 232 896 bytes  (el mismo que 0.1.16 y 0.1.15: el .deb cabe en los mismos sectores)
cmp p1 p2                   sin salida: IGUALES byte a byte
cmp p1 vs 0.1.16 (68cf0f44) distintas             <- el control: la comparación sabe decir que no
head -c $N       sobre el fichero  ->  3d5d12a9…  <- el instrumento de la receta, probado
head -c $((N-1)) sobre el fichero  ->  0b5a1af9…  <- el control N-1 de la receta, recalculado
/usr/bin/diff registros p1 p2      ->  solo los directorios de mktemp y el nombre de la salida
```

Las dos pasadas del **mismo** commit (a diferencia de §4.74c, que las hizo de
dos commits — aquello fue un control extra que hoy no hacía falta repetir).
El paso 3 de `construir-todo.sh` dio `6929df0e…` para el `.deb` reconstruido
desde HEAD en `encina-dev`: **la misma huella que la construcción de §4.76**
— dos construcciones, una huella. En cada pasada, el guion encendió la VM
comprobando que no había otra (trampa 14) y la apagó al terminar; el `[FALLO]`
intermedio de la cosecha es el control declarado del paso 4 (sin autofirma).

**Y una mordida nueva del instrumento, que amplía §4.9d:** el primer `diff` de
los dos registros contestó `[ok] Files are identical` — era **el hook de
`rtk`** resumiendo, no `diff`. Con `/usr/bin/diff` los registros difieren en
los mktemp, como en §4.74. La trampa «`git` puede contestar un commit que no
es» se queda corta: **`rtk` filtra también `diff`**, y la regla es la misma —
para medir, ruta absoluta o `rtk proxy`. (El `cmp` de las ISO no dependía de
esto: se leyó su código de salida, no su texto.)

#### (c) Dónde queda cada cosa

```
medios/encina-os-amd64.iso        3d5d12a9…   <- EL VIGENTE, con 0.1.17
medios/encina-os-amd64-0116.iso   68cf0f44…   <- el de §4.74, fabricado y NUNCA arrancado; su borrado es de Jorge
medios/encina-os-amd64-0115.iso               <- ya no está en medios/ (no lo borró esta sesión)
SHA256SUMS                        reescrito con lo que HAY, y verificado: shasum -c, 3 OK
```

La receta de §7 lleva `3d5d12a9…` y el control `0b5a1af9…`, con las
anteriores tachadas. La pasada 2 (`p2-0117.iso`) se borró tras el `cmp`: era
byte a byte la misma.

#### (d) Lo que NO contesta

- **Que arranque e instale.** Es la fila g, en el Acer, con este medio y SIN
  RED: la graba y la mira Jorge (paso 1 de la receta con las huellas nuevas).
- **El `[OJOS]` de la bellota en el hierro**: tras instalar, oscuro → claro →
  salir → entrar con `dconf read …/icon-theme` antes y después — es el control
  de producto de §4.76d y de la casilla de `aspecto/5-cierre.md`, y en el
  hierro se prueba «todo junto», que es exactamente lo que Jorge pidió.
- El `arm64` con 0.1.17: la fase 1b sigue pendiente y ahora le toca el 0.1.17,
  no el 0.1.16 (la vuelta única no se ha pagado aún — nada nuevo que pagar hoy).
