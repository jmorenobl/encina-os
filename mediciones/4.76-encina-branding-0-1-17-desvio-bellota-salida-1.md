# 4.76 `encina-branding` 0.1.17: EL DESVÍO DE LA BELLOTA — la salida (1) de §4.70c implementada, 26 de 26 con sus controles en la purga, y el control negativo de la trampa 13 cobrado a mano (2026-08-24)

**Qué.** La implementación que §4.75 dejaba viable: el SVG de Encina se sirve
también en la ruta de Yaru
(`/usr/share/icons/Yaru/scalable/actions/view-app-grid-ubuntu-symbolic.svg`)
sobre un `dpkg-divert --package encina-branding --rename` que registra el
**`preinst`** —antes del desempaquetado, que es el protocolo de
`dpkg-divert(1)` y el mecanismo que R5 prescribe— y retira el `postrm` en
`remove` (y `abort-install`/`disappear`; **nunca** en `abort-upgrade`, porque
la versión vieja sigue instalada y sigue necesitando el desvío). Se desvía
**el enlace** de Yaru, no su destino `start-here-symbolic.svg`. El fichero
sale de la misma fuente única que los dos de `Encina/` (`debian/rules`, tercer
`install -D`). El tema `Encina` y el override **no se tocan**: siguen siendo
la red. El porqué entero, en comentarios del `preinst`, como el drop-in de
0.1.16. Changelog con `dch -v 0.1.17` en `encina-dev`, traído al repo y
comprometido (`99c56a7`) **antes** de construir.

**Dónde.** `encina-dev`, por `ssh`, arrancada con las otras tres paradas
(trampa 14) y apagada al terminar. Identificada por huella antes de tocar
nada: `hostname` `encina-dev`, `encina-branding 0.1.16`, snap firefox
`153.0.4-1 rev 8753` — la misma máquina de §4.75. Construcción desde
`git archive HEAD` (§4.37), no del disco.

#### (a) Lo medido: construcción, instalación y la batería entera

```
03-construir.sh   38 correctas, 0 fallos; lintian sin errores (los dos W: del .desktop de la tienda, de antes)
encina-branding_0.1.17_all.deb   6929df0e69ac1c31c12a661a0ba35a038636983693fa3a00720afe742583ccd2
conffiles: los mismos 4 de 0.1.16 (el SVG de Yaru va bajo /usr: no es conffile, correcto)
04-instalar.sh    10 correctas, 0 fallos.  Y apt lo dijo literal:
    Añadiendo `desviación de /usr/share/icons/Yaru/.../view-app-grid-ubuntu-symbolic.svg
    a .../view-app-grid-ubuntu-symbolic.svg.distrib por encina-branding'
05-verificar.sh   26 correctas, 0 fallos, 0 avisos, 1 omitida (la conocida: el aviso de
                  deprecación de udev-settle solo sale tras reiniciar, §4.72)
```

La 2c nueva y sus cuatro respuestas con el paquete: `--listpackage` →
`encina-branding`; el fichero servido en la ruta de Yaru es el nuestro por
huella (`md5 7e26039f…`, la misma que el de `Encina/`); el original de Yaru
espera en `.distrib` como enlace a `../places/start-here-symbolic.svg` (con su
mtime de 2024 intacto); y `icon-theme.cache` de Yaru más nuevo que el SVG. Y
sus controles en la purga, los cuatro: desvío fuera, enlace original de vuelta
en su ruta, `.distrib` desaparecido, y la caché regenerada también al quitar.
Las cinco reinstalaciones (R9) con el desvío y la huella dentro de la
instantánea que se compara antes/después: idénticas.

#### (b) La trampa 13, pagada dos veces y con su control negativo a mano

**Quién regenera la caché de Yaru, medido y no supuesto:** no es un disparador
ajeno — §4.75c decía «el trigger de gtk debería» y era deducción—. Es el trozo
que `dh_icons` añade a **nuestro** `postinst` y `postrm`:

```
# Automatically added by dh_icons/13.14.1ubuntu5
	update-icon-caches /usr/share/icons/Encina /usr/share/icons/Yaru
```

(solo toca cachés que ya existen: la de Yaru sí, `Encina/` no lleva). Y el
instrumento de la 2c(iv) —«caché más nueva que el SVG»— demostró sus dos
respuestas en la misma máquina:

```
tras 05 (paquete puesto):        cache mas nuevo que SVG: SI
sudo touch del SVG:              cache mas nuevo que SVG: NO   <- sabe decir que no
reinstalar el .deb (restaura):   cache mas nuevo que SVG: SI
```

#### (c) De propina: `encina-dev` YA estaba en la condición del fallo

`dconf read /org/gnome/desktop/interface/icon-theme` del usuario `jorge` de la
VM devuelve **`'Yaru'`** — hay un valor de usuario escrito (leído por `ssh`,
que para un valor de usuario vale; lo que miente por `ssh` son los *defaults*
de `gsettings`). Quién lo escribió y cuándo no se sabe (`[OMIT]`), pero la
consecuencia es medible: en esa máquina el override ya estaba derrotado, y con
el desvío la bellota se sirve igual — que es exactamente el argumento de la
salida *(1)*.

#### (d) Lo que esta sección NO contesta

- **Qué escribe exactamente el panel en modo oscuro** (¿`Yaru-sage-dark`?),
  el pendiente de §4.75: exige tocar «Apariencia» en la sesión gráfica y un
  agente no pulsa botones del invitado (§4.35i). Queda impreso en el `[OJOS]`
  de `05-verificar.sh` con las órdenes exactas (`dconf read` antes y después).
- **El control de producto de la casilla** (`tareas/aspecto/5-cierre.md`):
  oscuro, claro, cerrar sesión, entrar, y que la bellota siga — `[OJOS]` de
  Jorge, la casilla **no se marca**. Y su negativo de §4.75c (sin desvío, la
  de Ubuntu) es exactamente lo que el Acer ya enseñó en §4.70c.
- **El medio vigente no lleva el 0.1.17**: el ritual de los seis sitios y la
  refabricación son de la fase del medio, no de hoy. `68cf0f44…` (amd64) y el
  arm64 siguen con 0.1.16.
- Los `[OJOS]` de siempre de `04-instalar.sh` (GRUB, splash, GDM, fondo): nada
  de hoy toca lo que pintan, y no se miraron.
